# Instagram fetch logic
def fetch_posts(keyword):
    # No-login scraping blocked
    raise RuntimeError(
        "Instagram scraping without login is blocked in 2025. "
        "Use a session-based login or paid API."
    )
